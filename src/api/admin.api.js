// import api from './axios'; // your configured axios instance

// export const adminApi = {
//   getRecentOrders: () => api.get('/admin/recent-orders'),

//   approveOrder: (orderId) => api.patch(`/orders/${orderId}/approve`),

//   getRecentUsers: () => api.get('/admin/recent-users'),

//   // ✅ Fix: use `api` not `axios`
//   getCourses: () => api.get('/courses/'),

//   approveEnrollment: (courseId, enrollmentId) =>
//     api.patch(`/courses/${courseId}/enrollments/${enrollmentId}/approve`),
// };
